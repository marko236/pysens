import os
import time
while True:
    os.system("clear") #optional, if you want to see only the latest output
    os.system("sensors") #shouldn't modify this
    time.sleep(1) #refresh rate in seconds
